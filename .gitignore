node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Claude Code — local/machine-specific & third-party tooling
.claude/settings.local.json
.claude/skills/

# Media assets (mp4 hero video is committed so CI/Pages can bundle it)
public/*.mov
public/*.webm
